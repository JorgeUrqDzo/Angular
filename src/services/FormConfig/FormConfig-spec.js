describe('FormConfig', function () {

    beforeEach(module('Controles'));

    it('should ...', inject(function (FormConfig) {

        //expect(FormConfig.doSomething()).toEqual('something');

    }));

});

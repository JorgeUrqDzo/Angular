describe('RutasConfig', function() {

    beforeEach(module('Controles'));

    it('should ...', inject(function($filter) {

        var filter = $filter('RutasConfig');

        expect(filter('input')).toEqual('output');

    }));

});
